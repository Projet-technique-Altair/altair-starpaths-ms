name: Starpaths MS CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

permissions:
  contents: read
  security-events: write

jobs:
  ci:
    uses: Projet-technique-Altair/altair-ci/.github/workflows/rust-ms.yml@main
    with:
      rust_toolchain: stable
